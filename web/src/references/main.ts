/* Boot for the references page. A static coming-soon page: only the theme
 * toggle and the footer year need JavaScript, both from the shared chrome. */

import { initTheme, initYear } from "../public/chrome";

initTheme();
initYear();
