node_modules/
dist/
src/wasm/pkg/
*.tsbuildinfo
# Copied from the repository root by the prebuild step, so the notices ship
# in the web build without a second tracked copy that could drift.
public/THIRD-PARTY-NOTICES.md
