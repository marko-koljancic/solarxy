{
  "name": "solarxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-wasm": "wasm-pack build ../ --target web --out-dir ./web/solarxy --release",
    "solarxy-build-run": "npm run build-wasm && npm run build && npm run preview",
    "preview": "vite preview"
  },
  "dependencies": {
    "solarxy": "file:solarxy"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.1"
  }
}
