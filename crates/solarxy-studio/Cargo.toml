[package]
name = "solarxy-studio"
description = "The shared interface derivation: how a node and its parameters are presented, written once for both shells."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
solarxy-graph = { path = "../solarxy-graph" }
solarxy-core = { path = "../solarxy-core", default-features = false }
serde = { version = "1.0", features = ["derive"] }
