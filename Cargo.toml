[package]
name = "solarxy"
description = "A lightweight, cross-platform 3D model viewer and validator built with Rust and wgpu. Inspect 3D models in a real-time graphical viewer or analyze them from the terminal with built-in validation checks."
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/marko-koljancic/solarxy"

[profile.release]
strip = true

[profile.dist]
inherits = "release"
lto = "thin"

[dependencies]
clap = { version = "4.5.53", features = ["derive"] }
anyhow = "1.0.100"
winit = "0.30.12"
wgpu = "27.0.1"
pollster = "0.4.0"
bytemuck = { version = "1.24", features = ["derive"] }
cgmath = "0.18"
tobj = { version = "4.0.3", default-features = false }
stl_io = "0.7"
wgpu_text = "27.0.1"
crossterm = "0.28.1"
ratatui = "0.29.0"
ply-rs-bw = "3.0.3"
gltf = "1.4"
chrono = "0.4.44"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
dirs = "6"

[dependencies.image]
version = "0.25.9"
default-features = false
features = ["png", "jpeg", "webp"]

[build-dependencies]
anyhow = "1.0"
fs_extra = "1.2"
glob = "0.3"
